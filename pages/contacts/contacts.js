// Define o título da página
var pageTitle = 'Faça Contato';

$(document).ready(runPage);

function runPage() {

    // Altera o título da página
    setTitle(pageTitle);

    console.log('Executando a Contacts');

}